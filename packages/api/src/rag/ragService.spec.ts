import { createRagService } from './service';import { chunkText } from './chunker';import { InMemoryVectorStore } from './vectorStores';import type { KnowledgeCollection, CollectionScope } from './types';function makeService() {  const ragCollections = new Map<string, KnowledgeCollection>();
  return createRagService({    config: {      chunkSize: 128,      chunkOverlap: 12,      hybridAlpha: 0.5,      candidateK: 5,      topK: 2,      minScore: 0.2,    },    knowledgeStore: {      createCollection: async (c: KnowledgeCollection) => { const col = c; ragCollections.set(col.id, col); return col; },      listCollections: async () => [],      getCollection: async (uid: string, id: string) => ragCollections.has(id) && ragCollections.get(id)!.userId === uid ? ragCollections.get(id) : null,      updateCollection: async () => null,      deleteCollection: async () => true,    } as never,    vectorStore: new InMemoryVectorStore() as never,  });}describe('rag service (B1.1)', () => {  it('creates a private collection with default chunk settings inherited from config', async () => {    const svc = makeService();    const collection = await svc.createCollection({      userId: 'u-1',      name: 'docs',      scope: 'private',    });    expect(collection.name).toBe('docs');    expect(collection.scope).toBe('private');    expect(collection.chunkSize).toBe(128);    expect(collection.documentCount).toBe(0);  });  it('rejects an empty collection name', async () => {    const svc = makeService();    await expect(      svc.createCollection({ userId: 'u-1', name: '', scope: 'private' }),    ).rejects.toThrow();  });  it('adds a document, chunks it, and persists both chunk and document counts', async () => {    const svc = makeService();    const collection = await svc.createCollection({      userId: 'u-1',      name: 'handbook',      scope: 'private',    });    const added = await svc.addDocument({      userId: 'u-1',      collectionId: collection.id,      source: 'manual.txt',      content: 'rag retrieves relevant chunks. rag chunks text into pieces.',    });    expect(added.chunkCount).toBeGreaterThan(0);    expect(added.documentId).toBeTruthy();  });  it('retrieves the most relevant snippet for a matching query', async () => {    const svc = makeService();    const collection = await svc.createCollection({      userId: 'u-1',      name: 'manual',      scope: 'private',    });    await svc.addDocument({      userId: 'u-1',      collectionId: collection.id,      source: 'manual.txt',      content: 'The rag service chunks documents and scores chunks by token overlap.',    });    const snippets = await svc.retrieve({      userId: 'u-1',      collectionId: collection.id,      query: 'rag service chunks',      topK: 2,      hybridAlpha: 0.5,    });    expect(snippets.length).toBeGreaterThan(0);    expect(snippets[0].score).toBeGreaterThan(0);  });  it('denies reading a collection the user cannot access', async () => {    const svc = makeService();    const collection = await svc.createCollection({      userId: 'u-1',      name: 'private',      scope: 'private',    });    const allowed = await svc.getCollection('u-2', collection.id);    expect(allowed).toBeNull();  });});